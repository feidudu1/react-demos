import React, {Component} from 'react';

import '../assets/block/SearchBar.less';

const SearchBar = () => {
    return (
        <div>
            <input type="text" placeholder="搜索框待写"/>
        </div>
    )
}

export default SearchBar;
